<div class="container hr">
  <div class="students_table">
    <%= render partial: "search/search_form" %>
    <h1><%= t('students.students') %></h1>
    <%= render partial: "students_listing" %>
  </div>
</div>